<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WComponent" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WComponent" parent="Base.Theme.WComponent" />


    <!-- Base application theme. -->

    <style name="MineAppTheme" parent="AppTheme">

    </style>


    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/color_text_main_17364E</item>
        <item name="android:windowBackground">@color/color_3B55E6</item>
        <item name="android:textAllCaps">false</item>
        <!--activity转场动画-->
        <item name="android:windowAnimationStyle">@style/activity_animation</item>
    </style>

    <style name="pictureTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowAnimationStyle">@style/activity_animation_zoom_in_zoom_out</item>
    </style>

    <!--全屏activity-->
    <style name="FullScreen" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
        <item name="windowNoTitle">true</item>
        <!--<item name="android:windowBackground">@mipmap/ic_logo</item>-->
    </style>

    <style name="dialogActivityTheme" parent="AppTheme">
        <!-- 去黑边 -->
        <item name="android:windowFrame">@null</item>
        <!--设置是否可滑动 -->
        <item name="android:windowIsFloating">true</item>
        <!--设置是否透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!--无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--背景 -->
        <item name="android:background">@color/transparent_80</item>
        <!--窗口背景 -->
        <item name="android:windowBackground">@color/transparent_80</item>
        <!--是否变暗 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!--点击空白部分activity消失 -->
        <item name="android:windowCloseOnTouchOutside">true</item>
        <!--动画设置 -->
        <item name="android:windowAnimationStyle">@anim/bottom_in</item>
    </style>
</resources>